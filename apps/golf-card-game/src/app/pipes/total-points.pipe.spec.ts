import { TotalPointsPipe } from './total-points.pipe';

describe('TotalPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
